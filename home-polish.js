(()=>{
'use strict';
/* Temporarily disabled. The previous version embedded a very large base64 JPEG
   directly in startup JavaScript, which could monopolize the iPhone main thread
   while the page appeared visible. Core app interaction takes priority. */
})();
